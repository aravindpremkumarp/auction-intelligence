# Copy this file to .env and fill in the real values. Never commit .env.

# FindAuction scraping credentials (LOCAL ONLY - not needed in production)
FINDAUCTION_EMAIL=
FINDAUCTION_PASSWORD=

# OpenRouter API (vision LLM for OCR + entity extraction).
# OPENROUTER_API_KEY funds the batch pipeline (scrape/OCR/classify/extract).
OPENROUTER_API_KEY=
# Dedicated key for the user-facing chat agent. Give it its own (smaller) credit
# cap on OpenRouter so a chat-abuse spike can't drain the pipeline budget. Falls
# back to OPENROUTER_API_KEY when blank, so single-key setups still work.
OPENROUTER_CHAT_API_KEY=
OPENROUTER_MODEL=google/gemini-2.0-flash-001
# Chat agent models (user-selectable toggle). Pro is the deeper-reasoning model
# (paid tier only); Flash is the cheap/fast model and the only option for
# free/anonymous chat. Reasoning effort is also a per-turn toggle (off/medium/
# high…); reasoning tokens bill as output, so it's the biggest cost lever.
OPENROUTER_MODEL_CHAT=deepseek/deepseek-v4-pro
OPENROUTER_MODEL_CHAT_FLASH=deepseek/deepseek-v4-flash
OPENROUTER_CHAT_REASONING_EFFORT=high

# Neo4j Aura
CLIENT_SECRET=
CLIENT_ID=
CLIENT_NAME=Bank Auction Intelligence
NEO4J_URI=neo4j+s://<your-instance>.databases.neo4j.io
NEO4J_USERNAME=
NEO4J_PASSWORD=
NEO4J_DATABASE=
AURA_INSTANCEID=
AURA_INSTANCENAME=

# OpenAI (backup LLM)
OPENAI_API_KEY=

# Google Gemini (required for semantic_search — gemini-embedding-2 vectors).
# Get a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Supabase Auth
# The backend verifies access tokens via JWKS at ${SUPABASE_URL}/auth/v1/.well-known/jwks.json.
# ANON key is safe to expose to the browser. Service role key is server-only; used by scripts/create_admin.py
# to look up a user by email before promoting them to admin in Neo4j.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Observability — Pydantic Logfire (OpenTelemetry tracing of the chat agent,
# LLM calls, and tool calls). Leave LOGFIRE_TOKEN blank to disable tracing
# (the app degrades to the structured logs in api/observability.py).
# Create a project + write token at https://logfire.pydantic.dev
LOGFIRE_TOKEN=
LOGFIRE_ENVIRONMENT=dev
# Optional: to send traces to a non-Logfire OTLP backend (LangSmith, Langfuse,
# Honeycomb, …) instead, leave LOGFIRE_TOKEN blank and set the standard OTel
# env vars, e.g. OTEL_EXPORTER_OTLP_ENDPOINT / OTEL_EXPORTER_OTLP_HEADERS.

# Evaluation (evals/run_golden.py) — judge model for the LLM-as-judge answer-
# quality score. Defaults to OPENROUTER_MODEL; set a stronger/independent model
# to reduce self-grading bias, e.g. anthropic/claude-sonnet-4.5.
EVAL_JUDGE_MODEL=

# App
APP_BASE_URL=http://localhost:5173
APP_ENV=dev
ADMIN_BOOTSTRAP_EMAIL=
AUTH_ENABLED=true
RATELIMIT_DISABLED=
# Durable, day-bucketed /chat quota per authenticated user. Free tier is the
# upgrade-pressure cap; paid tier (one-time time-boxed unlock) gets a higher,
# still-bounded cost-guard cap. Defaults: free=25, paid=1000.
CHAT_FREE_DAILY_LIMIT=
CHAT_PAID_DAILY_LIMIT=

# Razorpay paid-tier unlock (one-time, time-boxed). The webhook is the sole
# source of truth for activation; verify-on-return is UX only.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
# Unlock price in the smallest currency unit (paise for INR; default 49900 = ₹499).
RAZORPAY_PLAN_AMOUNT=
RAZORPAY_PLAN_CURRENCY=
# Unlock duration in days (default 30) and webhook idempotency TTL (default 90).
RAZORPAY_PLAN_DAYS=
RAZORPAY_WEBHOOK_TTL_DAYS=

# Pipeline (local batch runs)
# Log level for pipeline.obs structured logs (default INFO).
PIPELINE_LOG_LEVEL=
# Abort a batch LLM run once total tokens exceed this (unset = no cap).
PIPELINE_LLM_TOKEN_BUDGET=

# Cloudflare R2 (public bucket that serves auction sales notices to the web UI)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=auction-downloads
# Public base URL for the bucket (e.g. https://pub-xxxx.r2.dev or a custom domain).
# Objects are served at f"{R2_PUBLIC_BASE_URL}/{storage_key}".
R2_PUBLIC_BASE_URL=

# Private document dossier (feature-flagged). Ships dark: off by default so the
# public release doesn't expose it. Set true (1/yes/on) on a staging/test
# deploy to exercise it end-to-end — it gates the /dossiers API and the agent's
# query_user_dossier tool. The frontend has a matching ?dossiers=1 escape hatch
# (sticks in localStorage; ?dossiers=0 clears) to preview the UI on any deploy.
DOSSIERS_ENABLED=false

# Cloudflare R2 (PRIVATE bucket for confidential user dossier documents).
# Never made public — reads go through short-TTL presigned URLs. Uses the same
# R2 account credentials above; only the bucket name differs.
R2_PRIVATE_BUCKET=auction-dossiers

# MinerU OCR (https://mineru.net) — layout-aware OCR for notices + dossier docs.
MINERU_API_KEY=

# Doc-type classifier for the dossier locker (defaults to google/gemini-2.5-flash).
OPENROUTER_MODEL_DOC_CLASSIFY=

# Dossier ingest caps (synchronous upload→OCR→classify path).
DOSSIER_MAX_FILE_MB=10
DOSSIER_MAX_PAGES=15
