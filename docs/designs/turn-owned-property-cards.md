# Design: Turn-owned property cards

Generated by /office-hours on 2026-08-23
Branch: claude/managed-deep-agents-docs-ghqfvo
Repo: aravindpremkumarp/auction-intelligence
Status: APPROVED
Mode: Startup

## Problem Statement

Every agent surface (v1, v2, deep, agent3) shows *matches*, not
*recommendations*, and the matches panel loses sync with the conversation.
The user's own words from this session: "i was missing sync — ultimate goal
is to 100% sync with each chat with proper recommendation embedded in
property card."

Two distinct defects hide in that sentence:

1. **Sync.** v1/v2 *infer* what the panel should show by parsing six-digit
   ids out of the answer prose (`api/chat/panel.py::panel_sync_ids`). agent3
   fixed the data half — `ToolSink` records the exact match set — but the UI
   half is unchanged: one side panel, replaced on each turn, gone on reload,
   with no relationship to the message that produced it.
2. **Recommendation.** A card shows row data (bank, price, district). It does
   not show what the agent *said about that property this turn* — which is
   the only reason the user is looking at it.

## Decisions already made (this session)

- **No ranking layer.** The user explicitly chose "full matches, better
  presented" over a scored shortlist. We never invent a rank or score the
  agent didn't state — the same scope-honesty rule the gates enforce for
  text. A "recommended" card is one the agent *actually discussed*, not one
  an algorithm promoted.
- **Premise 1 — sync is structural.** What the UI shows always comes from
  the turn's manifest (sink-derived), never from parsing answer prose.
  `panel_sync_ids`-style inference is dead in this design.
- **Premise 2 — the card's recommendation is the agent's own words.** Each
  card carries the sentence(s) the agent wrote about that property this
  turn, quoted, plus hard graph facts. Attribution, not editorializing.
- **Premise 3 — panel state is part of the thread.** Per-turn manifests are
  persisted server-side with the conversation, so reload restores every
  turn's cards, and old turns remain viewable.

## The architecture: every assistant turn owns its cards

Chosen: **Approach B — cards in the chat stream** (user's selection, with
their own summary adopted as the spec).

The layering rule, verbatim from the session:

> LLM text = explanation.
> ToolSink/manifest = UI structure.
> Neo4j = authoritative property facts.
> Never parse the agent's prose to determine which cards to display.

### The Turn Manifest

One record per assistant turn, produced by `run_turn` alongside
`TurnResult`, persisted with the thread:

```
TurnManifest {
  thread_id:      str
  turn_index:     int          # see "turn_index provenance" below
  kind:           "search" | "detail" | "distribution" | "empty" | "none"
  produced_at:    datetime
  card_rows:      [dict]       # snapshot rows: auction_id + the volatile
                               # card fields only (reserve price, EMD
                               # deadline, possession type, headline sqft,
                               # district, scope) — NOT full graph rows
  discussed_ids:  [str]        # ids the answer names — extracted with the
                               # GATE's guarded extraction (ID_LIKE + ID_BAND
                               # + currency lookarounds, gates.py), exported
                               # as a shared helper. artifacts.cited_ids is
                               # the unguarded variant and is NOT used here.
  annotations:    {id: [str]}  # the answer's sentence(s) that name each id
  query_echo:     dict | None  # the find_properties args of the LAST search
                               # this turn ran (see multi-search rule)
  counts:         {total: int, shown: int}   # total = true match count from
                               # the tool's aggregation, a new ToolSink field
  breakdown:      dict | None  # kind=distribution only: the group_by table
  web_sources:    [dict]       # unchanged, for citation chips
}
```

**turn_index provenance.** The server computes it once, at manifest write
time, and never re-derives it: the count of *final* assistant messages in
the thread's checkpoint — AIMessages with no `tool_calls`, the same
finality test `AnswerGate.after_model` uses. (A naive AIMessage count is
wrong: a tool-using turn checkpoints several AIMessages, and gate repairs
insert extra messages; a turn that dies mid-flight can also leave partial
messages behind. Counting only final answers makes the ordinal equal the
number of answers the user actually received.) It is **1-based and
includes the answer this manifest describes** — at write time the current
answer is already checkpointed, so the first turn's manifest has
turn_index 1; the frontend's reload ordinal counts final assistant
messages the same way, so the two sides can never disagree by one. The
server-side thread is the source of truth for ordering — the client-saved history copy is
display-only and never used to join. The join key is `(thread_id,
turn_index)`; each chat response (and the SSE `manifest` event) carries
its own `turn_index` so the frontend tags the message it just rendered.
On reload, the frontend joins manifests to the server history fetch (a
new endpoint — see Dependencies) by the same ordinal. Any message with no
matching manifest renders card-less; ordinals never shift to compensate.

**Manifest kinds.** `search`: sink has rows. `detail`: no search ran,
`discussed_ids` non-empty (get_property / benchmark / reauction turns).
`distribution`: `find_properties` ran with `group_by` — the tool returns a
breakdown and absorbs no rows; the manifest carries the breakdown table and
renders it as such, never as "0 matches". `empty`: a search ran and matched
nothing. `none`: no search, no ids — a greeting, an IntentGate refusal
(which does emit a final answer and so does consume a turn_index), or a
web-only turn; carries only `web_sources` and renders no card strip.
"One record per assistant turn" holds without exception — every final
answer gets a manifest, so ordinals and manifests can never disagree.

**Multi-search turns.** `ToolSink.absorb` replaces `panel_rows`, so a turn
that searches twice keeps the last set. The manifest adopts that rule
explicitly: `card_rows`/`query_echo`/`counts` describe the LAST search of
the turn. A discussed id that appeared only in an *earlier* search of the
same turn is not in `card_rows` — its card facts come from the same by-id
fetch the `detail` kind uses, so it still renders a full card, never an
annotation with no facts.

**Annotation extraction is deterministic, not a model call.** Split the
final answer into sentences with, concretely,
`re.split(r"(?<=[.!?])\s+(?=[A-Z0-9₹(])", text)` applied per line;
markdown list items and headings are their own units. Digits are in the
lookahead ON PURPOSE — a sentence starting with an id must split — and
decimals are safe anyway because `₹1.5` has no whitespace after its dot.
Worked examples: `"748779 looks strong. 812440 is riskier."` → two units,
one per id. `"priced at ₹1.5 Cr. 812440 is next"` → splits after `Cr.`,
so 812440's card does not inherit the other property's price. `"- 748779:
symbolic possession"` → the list item is its own unit. Known imperfection,
accepted: `"748779 costs Rs. 50 lakh"` splits after `Rs.` (period +
whitespace + digit), so the id's unit is the truncated `"748779 costs
Rs."` — a clipped quote on the card, not a wrong attribution. Implementers
should treat clipped quotes as this documented limitation, not a bug;
tightening needs a `Rs`/`No` abbreviation lookbehind and is optional. A sentence that names a discussed id (via the gate's guarded
extraction) is attached to that id's card. Zero extra tokens, zero
latency, and the quote is verbatim — it cannot disagree with the chat
because it *is* the chat. A sentence naming two ids attaches to both.
Sentences naming no id attach to nothing.

Clarification of `discussed_ids` vs prose-parsing (this is not a violation
of premise 1): *which rows exist* comes only from the sink; *which of those
rows the agent talked about* comes from the answer — extracted with the
same hardened regex the AnswerGate already uses for hallucination checking.
An id in the answer that is NOT in the sink's rows or the thread's earlier
manifests is a gate matter and never renders a card.

### UI behavior

```
Turn 1  user: "3BHK under 60L in Coimbatore"
        agent prose
        ⭐ discussed:  [Card 748779] [Card 812440]
        ▸ all 21 matches (collapsed)

Turn 2  user: "only ones with physical possession"
        agent prose
        ⭐ discussed:  [Card 812440]
        ▸ all 6 matches (collapsed)
```

- Cards render **inside the assistant message that produced them**.
  Scrolling the conversation restores the correct cards by construction —
  there is no synchronization code because there is no shared mutable panel.
- A card shows, in order: the agent's quoted sentence(s) for this turn;
  hard facts from `panel_rows` (reserve price, EMD deadline, possession
  type, headline sqft, district); a **scope badge** when the notice is
  multi-lot (the UI inherits the graph's scope honesty — lot facts on a
  multi-lot notice are labeled as such, exactly as `get_property` labels
  them); link out to the listing.
- The collapsed "all N matches" section renders the full snapshot set as
  compact rows. The header carries the `query_echo` in words ("Coimbatore
  · ≤ ₹60L · physical possession") and the count delta from the previous
  turn's manifest ("21 → 6"). When the true total exceeds the sink cap the
  header says so — "showing 500 of 812" (`PANEL_ROW_CAP` is 500) — never a
  silent truncation.

### Storage and API

- **Persistence:** manifests live next to the conversation they belong to —
  a `TurnManifest` node keyed `(thread_id, turn_index)` in Neo4j, written by
  the same code path that already owns thread state (`Neo4jSaver` lives
  there; `DELETE /chat/agent3/{thread_id}` clears both, preserving the
  one-clear-site rule the design doc fought for). Neo4j properties cannot
  hold lists of maps, so `card_rows` / `annotations` / `breakdown` are
  stored as one JSON string property each. This is why `card_rows` is a
  *snapshot of card fields only*, not full graph rows: the duplication is
  bounded and deliberate — it freezes what the user was shown even if the
  listing later changes — and it is deleted with the thread (retention =
  thread lifetime, no separate story needed).
- **Write path:** `run_turn` returns `TurnResult` + manifest; the router
  persists the manifest before responding. Persistence failure must not
  fail the turn (same best-effort rule as `artifacts.py`).
- **Response shape:** `POST /chat/agent3` gains a `manifest` field carrying
  its `turn_index`. The stream endpoint (`POST /chat/agent3/stream`) emits
  a dedicated `manifest` SSE event immediately before its `final` event —
  same JSON body as the non-stream field. The existing `artifacts` list
  stays untouched during migration so the current panel keeps working until
  the new UI lands.
- **Reload:** `GET /chat/agent3/{thread_id}/manifests` returns all
  manifests for the thread, in turn order, alongside the existing history
  fetch. Access control is identical to the other `/chat/agent3` routes —
  `get_current_admin` today, and whatever ownership check the thread-history
  fetch adopts later applies here in the same commit. The frontend rebuilds
  every turn's cards from this — reload is indistinguishable from live.

### Turns that run no search

`get_property` / `benchmark_price` / `reauction_history` turns put nothing
in the sink. Their manifest is `kind: "detail"` with `card_rows` built from
a by-id fetch (the exact fallback `artifacts._fallback_artifacts` performs
today, reused). The collapsed "all matches" section is simply absent —
honest: this turn was not a search. Card eligibility uses the gate's own
grounding rule, which is deliberately **thread-wide**: an id is eligible
if it appears anywhere in the thread's tool outputs
(`gates.tool_output_text`) or in any manifest. So a fresh id the user
pasted into a `get_property` turn gets its card (this turn's tool result
grounds it), and an id from turn 1 discussed again in turn 5 does too.

### Edge cases (each is a test)

- **Empty result set** → the turn renders a "0 matches for: {query_echo}"
  card, not nothing. An empty state is a feature.
- **Answer cites an id from an *earlier* turn** → the card renders in the
  new turn with a "from turn N" marker; rows resolve from that earlier
  manifest, not a fresh guess.
- **Id in answer absent from this turn's tool outputs AND all manifests**
  → no card (gate territory); log it — it is a gate escape worth counting.
- **True total > PANEL_ROW_CAP (500)** → the collapsed section says
  "showing 500 of 812", never silently truncates (no-silent-caps rule).
- **Sentence-split failures** (abbreviations, unusual punctuation) → an
  annotation that can't be attributed cleanly attaches nothing; the card
  still renders with facts only. Annotation is enhancement, never a gate
  on rendering.
- **Two chats open on one thread** → both writers compute the same
  turn_index, so one manifest is silently lost while both assistant
  messages persist — a real misalignment, not a benign race. Recovery is
  the card-less rule above: a message with no matching manifest renders
  without cards; ordinals never shift to make later joins lie. The lost
  manifest is logged. (Preventing it outright needs per-thread write
  locking — out of scope, noted.)

## Approaches considered

- **A — smarter side panel** (manifest-driven, diff header, reload
  restore). Rejected in session: the panel/chat split — the root of the
  sync pain — survives; A cannot grow into B.
- **B — cards in the chat stream.** Chosen. Sync true by construction;
  history is the record. Effort: human ~1 wk / CC ~2 sessions.
- **C — dual-plane time travel** (B + persistent workspace panel, pinning,
  cross-turn shortlist). Deferred, not rejected: C grows out of B; pinning
  and a workspace panel layer on top of manifests without schema change.

## Open Questions

1. Does the tiered (v1/v2) surface adopt manifests too, or is this
   agent3-only until agent3 becomes the default loop? (Recommendation:
   agent3-only; v1/v2 keep `panel_sync_ids` until retired.)
2. Card *visual* design — density, thumbnails, mobile layout — is
   deliberately out of scope; it goes to /plan-design-review after this
   doc. The *structural* frontend change is in scope and small in surface:
   `web/app.js` renders a card strip inside each assistant message bubble
   from the response's `manifest` (new render function beside the existing
   `extractResultsFromArtifacts` path), the stream handler consumes the
   `manifest` SSE event, and thread-open calls `/history` + `/manifests`
   and joins by `turn_index`. The old side-panel path stays behind the
   loop switch until agent3's new UI is default.
3. Should `query_echo` render technical filter names or natural language?
   (Leaning natural language generated deterministically from args.)

## Success Criteria

- Reloading a thread reproduces every turn's cards exactly — verified by a
  test that runs 3 turns, refetches manifests, and diffs.
- No code path in the display pipeline parses prose at render time:
  verified by a test that renders a stored manifest after deleting the
  thread's message history — cards AND annotations must both be intact,
  because both live in the manifest; nothing is re-derived from prose
  after the write.
- Each assistant message's cards derive solely from its own manifest —
  verified by rendering turn N's cards with all other manifests deleted.
- A multi-lot notice's card shows its scope badge — the UI can never state
  a lot fact as a property fact.

## Dependencies

- **ToolSink additions (counted as part of this work, not free):** `total`
  (the true match count from the aggregation), `query_args` (the last
  search's arguments for `query_echo`), `breakdown` (the group_by table —
  today it goes only to the model's tool message, never the sink), and
  absorbs on the zero-result and `group_by` paths of `find_properties`,
  which today skip the sink entirely. Plus a shared export of the gate's
  guarded id extraction.
- **New endpoint `GET /chat/agent3/{thread_id}/history`** — does not exist
  today (the router has only POST, POST /stream, DELETE; history is a
  client-saved copy, which this design bans from joins). It returns the
  thread's final assistant/user messages in ordinal order and ships in the
  same change as `/manifests`; the reload join is between these two
  server responses.
- Deferred-but-related: the diligence dossier ("not now", 2026-08-23 CEO
  review) would hang off these same cards later; vector search changes
  what `query_echo` contains but not the manifest shape.

## The Assignment

Before any code: take your own 7-turn transcript from last week, and for
each turn write down (pen and paper is fine) which cards SHOULD have been
showing. That paper trace is the acceptance fixture for the manifest tests
— it turns "100% sync" from a slogan into a checkable artifact.

## What I noticed about how you think

- You answered the architecture question by writing the spec yourself —
  "Every assistant turn owns its cards… synchronization true by
  construction" — that's you compressing a week of design into a rule.
- You rejected the ranked shortlist even though it was the flashier
  option, because it would invent judgment the agent didn't state. Same
  instinct as "keep internet search general, not hard-coded rules."
- You named the goal in one sentence with a measurable word in it: "100%
  sync." Designs go better when the founder supplies the invariant.
