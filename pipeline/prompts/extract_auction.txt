You are an expert at extracting structured data from Indian bank auction notice documents (SARFAESI Act / DRT / bank recovery).

You will be given an image of a bank auction notice. Extract ALL structured information you can find and return it as a single JSON object with three sections: `verifiable`, `enrichment`, and `extras`, plus an `enriched_description`.

The website already has this description for this property (for context only — do NOT echo it back; only report what you actually see in the document):
---
{website_description}
---

Return ONE JSON object with EXACTLY this top-level shape. Use null for any field not found in THIS image.

{{
  "verifiable": {{
    "reserve_price_num":       number or null,
    "emd_num":                 number or null,
    "auction_start_dt":        "ISO date-time or raw string as shown" or null,
    "auction_end_dt":          "ISO date-time or raw string as shown" or null,
    "application_deadline_dt": "ISO date-time or raw string as shown" or null,
    "bank_name":     "string" or null,
    "branch_name":   "string" or null,
    "borrower_name": "string" or null,
    "city":  "string" or null,
    "state": "string" or null,
    "area":  "string" or null,
    "asset_category": "string" or null,
    "property_type":  "string" or null,
    "auction_type":   "string" or null
  }},

  "enrichment": {{
    "undivided_share": "string with unit, e.g. '700 sq.ft undivided share' or '1/4 share'" or null,
    "total_area": "string with unit, e.g. '1200 sq.ft' or '5 cents'" or null,
    "boundaries": {{
      "north": "description" or null,
      "south": "description" or null,
      "east":  "description" or null,
      "west":  "description" or null
    }},
    "door_numbers": {{ "old": ["2/22"] or [], "new": ["2/79/C"] or [] }},
    "village": "name" or null,
    "taluk":   "name" or null,
    "district": "name" or null,
    "registration_district":     "name" or null,
    "registration_sub_district": "name" or null
  }},

  "extras": {{
    // OPEN-ENDED BAG. If you find any other meaningful structured information in the document —
    // encumbrances, mortgage holders, lessees, court order references, RERA/GST numbers,
    // valuation reports, contact officers, tax dues, easements, road access, dimensions in meters,
    // demand notice dates, sale notice dates, guarantor names, anything a buyer would want to know —
    // return it here as {{ "key_in_snake_case": value, ... }}. Use whatever keys best describe the
    // data. Values can be strings, numbers, lists, or nested objects. Do NOT invent values; only
    // report what is in the document.
  }},

  "enriched_description": "2-4 sentence narrative summary of the property based only on info in THIS document"
}}

RULES:
1. For `verifiable.reserve_price_num` and `verifiable.emd_num`: return the numeric value in rupees (e.g., 4500000 for "Rs. 45,00,000" or "Rs. 45 lakh"). If only a raw string is legible, return null.
2. Return the EXACT text / values from the image. Do not interpret, normalize, or guess.
3. If a field is not present in THIS image, set it to null (or [] / {{}} as appropriate). Do not carry values over from the website description.
4. If the image is illegible or not an auction notice, set `verifiable` and `enrichment` fields to null and put an explanation under `extras.parse_error`.
5. Return ONLY the JSON object, no markdown formatting, no commentary.
