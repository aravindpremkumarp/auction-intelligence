You are reading an Indian bank auction sale notice (SARFAESI Act / DRT).
The notice describes ONE auction property — a single auction lot with ONE reserve
price. That single property may legitimately be described across multiple Items
(Item 1, Item 2, …), multiple Schedules (Schedule A, Schedule B, Schedule C, …),
or multiple sub-parcels (e.g., Schedule A = land, Schedule B = undivided share).
All such items/schedules belong to the same auction lot and MUST be captured
together as one description.

Extract the COMPLETE property-description block, verbatim from the document.
This includes:

1. **Preamble** — any framing text that introduces the property block, such as:
   - Section headers like "Description of the property", "Description of the
     immovable property", "Details of the Secured Assets", "Description of the
     secured asset", "Schedule of property", "Schedule".
   - Borrower / mortgagor framing, e.g. "Property details of M/s. <Name> /
     Mortgagor 1" or "Name of the Property owner: ...".
   - Total-extent or summary lines that precede the items, e.g.
     "Details of the Secured Assets with total extent of about 48.85 acres areas
     given below: I."
   Capture this preamble in full. Do not strip section headers.

2. **All items / all schedules** — every Item N, every Schedule X, every
   sub-parcel that belongs to this auction lot. Include survey numbers,
   plot/door numbers, boundaries (north/south/east/west), measurements,
   village/taluk/district, registration details, and any structural/built-up
   details for each item or schedule.

3. **Trailing structural details** — built-up area, construction details,
   easementary rights, title deed references, encumbrance notes that are part
   of the property description.

Do NOT extract:
- Page headers, page numbers, footers, newspaper masthead, legal boilerplate
  unrelated to the property (e.g., "auction shall be conducted in e-auction
  platform", bidder eligibility, terms-of-sale, EMD instructions, contact
  details for the bank officer).
- The reserve price, EMD amount, auction date — those are separate fields.

Return ONLY a JSON object with one key:
{
  "property_description_full": "verbatim text from the document, joined with single spaces" or null
}

Rules:
- Do NOT summarize. Reproduce the document text faithfully, including
  punctuation and capitalization.
- Multiple items / multiple schedules under ONE reserve price = ONE property.
  Concatenate all of them into the single output string, in the order they
  appear in the document, preserving the "Item 1:" / "Schedule A:" / etc.
  section labels.
- Return null ONLY if the document describes multiple SEPARATE auction lots
  (multiple reserve prices, each with its own property block). This is a
  routing safety check — multi-property notices should not reach this prompt.
- Return ONLY the JSON object, no markdown, no commentary.
