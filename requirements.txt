# Production requirements for Render deployment.
# Excludes scraping (selenium, webdriver-manager) and PDF/image generation
# (weasyprint, pdf2image, Pillow) which are local-only features.
# For full local dev requirements see config/requirements.txt.

# Core utilities
python-dotenv
aiohttp
rapidfuzz
pyyaml

# Database
neo4j

# LLM clients
google-genai

# API + Agent layer
fastapi
uvicorn[standard]
pydantic-ai

# Auth + email
bcrypt
PyJWT
httpx
slowapi
email-validator

# Web search
tavily-python
