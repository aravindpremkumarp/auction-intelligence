# Legacy scrapers

Superseded variants kept for reference, not maintained:

- `eauctions_scraper.py` — original Selenium scraper for eauctionsindia.com,
  replaced by the requests-based `scrapers/fast_eauctions_scraper.py` (the
  canonical entry point, see the README pipeline steps).
- `explore_eauctions.py` — one-off exploration probe of the site structure.

Delete this directory when confident nothing here is needed again.
