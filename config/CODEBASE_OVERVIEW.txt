# Bank Auction Intelligence — Codebase Overview

AI-powered intelligence system for Indian SARFAESI Act bank property auctions.
Starts with a scraper for `eauctionsindia.com`, builds a Neo4j knowledge graph
of Tamil Nadu auctions, enriches it via OCR + vision-LLM extraction, and
serves a PydanticAI agent (FastAPI + single-page web UI) that scores, tracks,
and captures user feedback on investment opportunities.

## High-Level Flow

    scrape → clean/filter TN → load Neo4j → OCR+extract →
    verify/enrich → lexical graph → normalize → load enriched →
    embed descriptions → score/track → serve via API → feedback loop

## Top-Level Layout

- `README.md` — Quick-start, deployment, project map.
- `requirements.txt` — Runtime deps for Render web service (slim).
- `render.yaml` — Render deployment config (FastAPI backend).
- `vercel.json` — Vercel deployment config (static `web/` frontend).
- `.env.example` — Template for local secrets (OpenRouter, Neo4j Aura,
  FindAuction scraping creds). Never commit the filled-in `.env`.
- `.github/workflows/` — CI/CD:
    - `ci.yml` — Runs `pytest tests/api` on every PR / push to main.
    - `resolve-feedback.yml` — On merged PRs, parses
      `Resolves feedback: <uuid>` markers from the PR body and PATCHes
      `/feedback/<id>/resolve` on the live API.
    - `sync-feedback.yml` — Every 15 min (and on manual dispatch)
      snapshots `/feedback/recent` into `feedback/all.json` +
      `feedback/unresolved.json` and commits the diff.
    - `golden.yml` — Nightly (02:00 UTC) live eval that runs
      `tests/api/test_golden_questions.py` against the real
      OpenRouter + Neo4j Aura stack. Requires the `OPENROUTER_*`,
      `NEO4J_*`, and `OPENAI_API_KEY` secrets.
- `config/` — Schema + connection + full dev requirements + this document.
- `feedback/` — Auto-synced snapshots of live feedback (see below).
- `api/`, `pipeline/`, `scrapers/`, `scripts/`, `scoring/`, `tracking/`,
  `modes/`, `web/`, `tests/`, `walkthrough/` — see below.

## config/

- `domain_ontology.yaml` — Canonical entity/relationship/property schema for
  the whole system (AuctionProperty, Bank, Branch, City/State/Area,
  AssetCategory, PropertyType, Borrower, AuctionType, + enriched fields:
  village/taluk/district, total_area, undivided_share, boundaries, etc.).
- `auction_graph_model.json` — Arrows.app visual graph model (mirrors ontology).
- `requirements.txt` — Full development dependencies (scraping, OCR, tests).
- `CODEBASE_OVERVIEW.txt` — This document.
- Neo4j Aura credential files are kept locally and `.gitignore`d.

## 1. Scraping Layer (`scrapers/`)

- `fast_eauctions_scraper.py` — Primary multi-threaded scraper. Crawls
  pagination, dedupes against existing data, parses detail pages with
  BeautifulSoup, downloads linked PDFs to `downloads/live_properties/`, and
  appends to JSONL.
- `eauctions_scraper.py` / `scraper.py` — Earlier single-threaded versions.
- `explore_eauctions.py` — Site exploration helper.
- `scraper_controls.py`, `utils.py` — Shared scraper helpers.
- `live_eauction_data.jsonl` / `.csv` — Raw all-India dataset (~49K records
  at last run).
- `scrape_errors.log` — Scraper error log.
- `downloads/` — Downloaded auction documents (PDFs, images).

## 2. Diagnostic & Data-Prep Scripts (`scripts/`)

Mixed bag of one-off utilities and the TN data-prep pipeline.

- `prepare_tn_data.py` — Filters raw JSONL to Tamil Nadu, normalizes prices
  (₹/commas → float), dates (DD-MM-YYYY HHMM AM/PM → ISO 8601), and
  downloads list. Validates file existence → `downloads_complete` flag.
  Produces `tn_auction_data.jsonl` (~4,788 cleaned TN records at last run,
  primary input for Neo4j ingestion) and `tn_download_report.txt`.
- `load_tn_to_neo4j.py` — Creates constraints/indexes and batch-MERGEs nodes
  and relationships into Neo4j Aura.
- Property-type schema repair (one-shots from PR #9):
    - `inspect_property_type_edges.py` — Diagnose edge-direction bugs
      by counting `HAS_PROPERTY_TYPE`, `OF_PROPERTY_TYPE`, and
      `HAS_TYPE` edges in the live graph.
    - `migrate_property_types.py` — Delete stale comma-joined
      `PropertyType` nodes and the legacy `AssetCategory-[:HAS_TYPE]->`
      edges; rebuild per-auction type attribution.
    - `restore_has_property_type.py` — Replay
      `tn_auction_data.jsonl` into prod to rebuild missing
      `(AuctionProperty)-[:HAS_PROPERTY_TYPE]->(PropertyType)` edges
      and clean up stray `OF_PROPERTY_TYPE` edges.
    - `smoke_check.py` — Post-repair sanity: verifies
      "Chennai Flats" / "Kanchipuram Flats" return the expected
      cardinality.
- Diagnostic/utility scripts: `check_*.py`, `debug_*.py`,
  `fuzz_pagination.py`, `find_count_selector.py`, `verify_download_fix.py`,
  `fix_extensions.py`, `compare_urls.py`, `remove_duplicates.py`,
  `dump_text.py`, `diagnose_stuck.py`, `get_keys.py`,
  `convert_to_excel.py`.

## 3. Enrichment Pipeline (`pipeline/`)

Multi-stage pipeline that turns downloaded auction PDFs into enriched graph
data.

- `config.py` — Central config: OpenRouter keys/model, Neo4j creds, scoring
  weights, decision thresholds, path constants, tracking TSV location.
- `run_pipeline.py` — Orchestrator. Runs the stages sequentially. Flags:
  `--limit N` (OCR subset), `--skip-ocr`.
- `ocr_extract.py` — Stage 1: OCRs PDFs and calls a vision LLM
  (OpenRouter / Gemini 2.0 Flash) with `prompts/extract_auction.txt` to
  extract structured auction fields. Caches per-file results under
  `cache/ocr_results/` and writes `output/extracted.jsonl`.
- `verify_and_enrich.py` — Stage 1.5: Verifies scraped fields against PDF
  extractions per auction. PDF wins on conflicts (original preserved as
  `<field>_scraped`, name added to `field_conflicts`); `enrichment.*` is
  merged, open-ended `extras` is shallow-merged. Outputs
  `output/verified_enriched.jsonl` + `output/extras_key_frequency.json`.
- `lexical_graph.py` — Stage 2: Builds a lexical graph over extracted text
  (chunks + entity mentions). Writes `output/lexical.jsonl`.
- `normalize.py` — Stage 3: Normalizes entity names (banks, locations,
  property types) against `lookups/bank_names.json`, `locations.json`,
  `property_types.json`. Writes `output/normalized.jsonl` +
  `output/normalization_report.txt`.
- `load_enriched.py` — Stage 4: Merges enriched properties, survey details,
  possession info, and geographic hierarchy into Neo4j.
- `embeddings.py` — Shared text-embedding helper (batched OpenAI embeddings,
  exports `EMBED_DIM`).
- `embed_descriptions.py` — One-shot backfill: embeds
  `AuctionProperty.description` for every property, writes the vector back
  as `p.embedding`, and creates the `property_desc_idx` vector index. Safe
  to re-run; skips existing unless `--force`.
- `cache/` — Per-document cache for OCR/LLM calls (expensive to recompute).
- `prompts/extract_auction.txt` — Prompt template for LLM extraction.

## 4. Scoring (`scoring/`)

- `auction_scorer.py` — Ten-dimensional investment scoring. Each dimension
  returns 0–100; composite is weighted avg per `SCORING_WEIGHTS`. Persists
  `Score` nodes on properties in Neo4j.

Dimensions (weights in `modes/_shared.md`):
  A Price Attractiveness 20% · B Location Quality 15% ·
  C Legal Clarity 15% · D Bank Reliability 10% ·
  E Property Condition 10% · F Timeline Urgency 10% ·
  G Due Diligence Ease 5% · H Area Price Trend 5% ·
  I Competition Risk 5% · J Yield Potential 5%

## 5. Tracking (`tracking/`)

- `auction_tracker.py` — Eight-state investment pipeline:
  DISCOVERED → SCORED → SHORTLISTED → RESEARCHING → BID_READY →
  BID_SUBMITTED → WON/LOST → COMPLETED.
  Transitions past SCORED require human-in-the-loop confirmation.
  Persists `InvestmentTracker` nodes in Neo4j + TSV mirror.

## 6. Agent API (`api/`)

- `main.py` — FastAPI app. Serves `/web` static UI and agent + feedback
  endpoints. Run: `uvicorn api.main:app --reload`.
  Endpoints:
    - `GET  /health` — Liveness probe.
    - `GET  /health/deep` — Extended health: verifies Neo4j
      connectivity, `AuctionProperty` count, and presence of the
      `property_desc_idx` vector index.
    - `GET  /modes` — Mode registry consumed by the UI mode selector
      (id, label, description, example prompts per mode).
    - `POST /chat` — Agent turn; accepts optional `mode` for mode
      overlay. Returns answer, tool artifacts, and full
      `message_history` for the client to replay on the next turn.
    - `GET  /auction/{auction_id}` — Full auction detail for the
      detail screen in the UI (wraps `cypher_tools.get_auction_detail`).
    - `POST /feedback` — Persist a thumbs up/down on an assistant reply.
      Captures the question, answer, tool artifacts, user-agent, and a
      trimmed window of prior `context_turns` (role + content + tool
      calls, content capped at 2000 chars). Written as `:Feedback` nodes
      in Neo4j.
    - `GET  /feedback/recent` — List recent feedback; filters by
      `unresolved_only` and `rating`.
    - `PATCH /feedback/{id}/resolve` — Mark feedback resolved. Guarded by
      the `X-Resolve-Token` header matching `FEEDBACK_RESOLVE_TOKEN`;
      called automatically by the `resolve-feedback.yml` workflow when a
      fix PR is merged.
- `agent.py` — PydanticAI agent wired to OpenRouter (Gemini 2.0 Flash).
  System prompt is assembled at boot from a short `_ROLE_PROMPT` plus
  the full `modes/_shared.md` (schema, enum lists, tool-choice rules,
  Cypher cheat-sheet). Two dynamic system prompts augment each turn:
  `inject_prior_search` (surfaces the last tool's filters + total
  count for conversational continuity) and `inject_mode_overlay`
  (appends `modes/<mode>.md` when the client passes a `mode`).
- `neo4j_client.py` — Shared Neo4j driver. `session()` / `run_query()`
  for writes/reads, plus `read_session()` and `run_read_query()`
  which force `READ_ACCESS` mode, accept a server-side `timeout`
  (default 10 s), and trim results to `max_rows` — the safety net
  behind the `run_cypher` escape hatch.
- `tools/cypher_tools.py` — Agent tools (each registered via
  `@agent.tool_plain` in `api/agent.py`):
    - `search_auctions` — Filter by price / city / area / property
      type / asset category / date window; supports aggregate
      statistics (min/max/avg/median/p25/p75) over the full filtered
      set and returns a true `total_count`.
    - `find_similar_properties`, `bank_portfolio`, `location_analysis`,
      `price_comparison`, `get_auction_detail` — Specialized parameterized
      Cypher queries. (borrower and upcoming-deadline queries are now
      `search_auctions` params: `borrower`, `deadline_within_days`.)
    - (scoring, watch, and alerts are NOT chat tools — the 10-dim
      scorer lives in `scoring/auction_scorer.py` and watchlist/alerts
      are UI features served by their REST routers.)
    - `semantic_property_search` — Vector search over
      `AuctionProperty.embedding` via the `property_desc_idx` index,
      with optional city / price / asset-category post-filters.
    - distributions via `search_auctions(group_by=...)` — Enum discovery
      ("what cities do we cover", "which property types appear in
      Chennai", "what auction types do we have"). `field` ∈ {city,
      area, state, bank, branch, borrower, asset_category,
      property_type, auction_type}. Scope filters: city, bank,
      borrower, asset_category, auction_type, branch.
    - `describe_schema(refresh=False)` — Introspect the live graph
      (labels + counts, relationship types, enum values, numeric /
      date ranges of key fields). 1-hour in-memory cache. Called
      before composing a `run_cypher` query for a novel question.
    - `run_cypher(cypher, params, description, max_rows)` —
      Read-only escape hatch for questions the specialized tools
      cannot express. Regex guards reject `CREATE`, `MERGE`,
      `DELETE`, `SET`, `REMOVE`, `DROP`, `LOAD CSV`, `FOREACH`, and
      write-side procedures; query text capped at 4000 chars,
      server-side execution capped at 10 s, and rows capped at
      `max_rows` (default 200, hard max 500). Backed by
      `neo4j_client.run_read_query`.

## 7. Modes (`modes/*.md`)

Markdown "mode" prompts the agent can switch into:
- `_shared.md` — Loaded into the system prompt at boot. Defines the
  full Neo4j schema (node labels + key properties, relationships,
  enum constraints), tool-choice rules, and a Cypher cheat-sheet for
  `run_cypher`.
- `deep-research.md` — 3-phase due diligence on one auction (in-chat
  report): snapshot → one batched parallel evidence sweep → write-up.

Only `deep-research` (plus the default `ask`) is wired into
`_AVAILABLE_MODES` in `api/chat/router.py`. Seven further specs — `scan`,
`shortlist`, `evaluate`, `track`, `refresh`, `compare`, `report` — are
parked in `modes/_archive/` (inert; never loaded). See
`modes/_archive/README.md`.

## 8. Web UI (`web/`)

- `index.html` — Single-page "mid-fi sketch" UI served by FastAPI and
  deployed statically to Vercel. No build step, no frameworks: vanilla
  JS with hand-rolled CSS variables and hand/mono fonts (Caveat,
  Kalam, IBM Plex Mono via Google Fonts). Four-screen flow:
    1. **Landing / search** — Hero prompt input, mode dropdown
       (populated from `GET /modes`), suggestion chips.
    2. **Results** — Sidebar (chat history) + transcript pane
       (per-bubble thumbs-up/down feedback) + results pane
       (property rows from `search_auctions` /
       `semantic_property_search`).
    3. **Detail** — Back link, title, photos, fact grid,
       description, documents, save-to-watchlist button, and a
       per-property chat box. Loads from `GET /auction/{id}`.
    4. **Watchlist** — Saved properties grouped by timeline
       urgency (red for imminent auctions with a countdown).
  Header carries the brand logo, mode selector, a general-purpose
  feedback button (`kind="general"` POST to `/feedback`), saved-count
  badge, and avatar. The `API_BASE` is resolved at runtime — empty on
  `localhost`, otherwise the hosted Render URL.

## 9. Tests (`tests/`)

- `tests/api/` — FastAPI endpoint tests (run in CI).
    - `conftest.py` — In-memory stub for `run_query` /
      `run_read_query` so tests don't need a live Neo4j.
    - `test_feedback.py` — End-to-end `/feedback`, `/feedback/recent`,
      and `/feedback/{id}/resolve` coverage (including `context_turns`
      capture and resolve-token guard).
    - `test_import.py` — Import smoke test.
    - `test_cypher_tools_area.py` — Verifies the `area` filter in
      `search_auctions` (case-insensitive substring on `Area.name`).
    - `test_describe_schema.py` — Validates `describe_schema()`
      output shape (labels + counts, relationships, enums,
      numeric/date ranges) against the stubbed queries.
    - `test_group_by.py` — Verifies the distribution Cypher shape + param
      wiring for enum discovery, including city-scoped variants.
    - `test_run_cypher_guardrails.py` — Rejects write clauses /
      overlong queries / unsupported params; confirms safe read
      queries pass through and are row-capped.
    - `test_golden_questions.py` — Catalogue of realistic end-user
      questions grouped by intent. Under `RUN_LIVE_EVAL=1` it calls
      the real OpenRouter + Neo4j stack and asserts the agent picks
      an acceptable tool per question; nightly via `golden.yml`
      (skipped in the regular PR CI).
- Top-level `test_*.py` — Scraper / networking probes (`test_cf.py`,
  `test_cloudscraper.py`, `test_curl_cffi.py`, `test_httpx.py`,
  `test_nonheadless.py`, `test_pattern.py`, `test_pw.py`,
  `test_selectors.py`, `test_uc.py`). Local-only; not run in CI.

## 10. Reports

The `compare`, `deep-research`, and `report` modes return their output as
in-chat markdown (no files). `reports/output/` is a legacy path from the
CLI-era design and is not produced by the web agent.

## 11. Walkthrough (`walkthrough/`)

- `walkthrough.md` — Log of scraper execution sessions and dataset-sync
  verification steps.

## 12. Feedback Snapshots (`feedback/`)

Read-only snapshots of the live `/feedback/recent` feed, committed
back into the repo by `.github/workflows/sync-feedback.yml` every
15 minutes so maintainers can browse and diff feedback in-repo.

- `all.json` — Last 200 feedback records (all statuses), sorted by
  `created_at` descending.
- `unresolved.json` — Same query with `unresolved_only=true`.

## Neo4j Graph Model

Nodes: `AuctionProperty`, `Bank`, `Branch`, `City`, `State`, `Area`,
`AssetCategory`, `PropertyType`, `Borrower`, `AuctionType`,
plus enrichment nodes (`Score`, `InvestmentTracker`, `Feedback`).

Key relationships:
  (AuctionProperty)-[:CONDUCTED_BY]->(Bank)
  (Bank)-[:HAS_BRANCH]->(Branch)
  (AuctionProperty)-[:LISTED_BY_BRANCH]->(Branch)
  (AuctionProperty)-[:LOCATED_IN_CITY|LOCATED_IN_STATE|LOCATED_IN_AREA]->(...)
  (Area)-[:PART_OF_CITY]->(City)-[:IN_STATE]->(State)
  (AuctionProperty)-[:HAS_ASSET_CATEGORY]->(AssetCategory)
  (AuctionProperty)-[:HAS_PROPERTY_TYPE]->(PropertyType)   # one-to-many
  (AuctionProperty)-[:HAS_BORROWER]->(Borrower)
  (AuctionProperty)-[:IS_AUCTION_TYPE]->(AuctionType)

`AuctionProperty.embedding` is indexed as the `property_desc_idx` vector
index (built by `pipeline/embed_descriptions.py`) for semantic search.

See `domain_ontology.yaml` for the full property-level schema.

## Deployment

- **Frontend** → Vercel (serves `web/` as a static site; config in
  `vercel.json`). The production API base URL is hard-coded in
  `web/index.html`.
- **Backend** → Render Python web service (config in `render.yaml`,
  installs the slim root `requirements.txt`).
- **Database** → Neo4j Aura (hosted).

## Typical End-to-End Run

    python -m scrapers.fast_eauctions_scraper    # 1. Scrape
    python -m scripts.prepare_tn_data            # 2. Clean TN subset
    python -m scripts.load_tn_to_neo4j           # 3. Load base graph
    python -m pipeline.run_pipeline              # 4. Enrich (OCR→verify→
                                                 #    lexical→normalize→load)
    python -m pipeline.embed_descriptions        # 5. Backfill embeddings
    uvicorn api.main:app --reload                # 6. Serve agent + web UI

## Feedback Loop (CI/CD)

1. User clicks thumbs down on a reply in the web UI.
2. Frontend POSTs to `/feedback` with question + answer + tool artifacts
   + trimmed prior `context_turns`.
3. A `:Feedback` node is stored in Neo4j (`resolved: false`).
4. Maintainers review via `GET /feedback/recent?unresolved_only=true`.
5. Fix PRs include `Resolves feedback: <uuid>` in the body.
6. On merge, `.github/workflows/resolve-feedback.yml` PATCHes
   `/feedback/<uuid>/resolve` (guarded by `FEEDBACK_RESOLVE_TOKEN`) so
   the item drops off the unresolved queue.

## Last Execution Status

- Scraper (`scrapers/fast_eauctions_scraper.py`) was run and verified to
  be fully up-to-date: 0 new URLs were found against the existing 49,342
  extracted entries, meaning `live_eauction_data.jsonl` contains the most
  current data.
- Tamil Nadu subset currently sits at **3,391** records in Neo4j
  (the figure the agent cites in its system prompt); the earlier
  4,788 figure was pre-deduplication / pre-`HAS_PROPERTY_TYPE`
  schema repair.
- PR #9 (`feature/area-filter-and-cleanup`) restored the
  `(AuctionProperty)-[:HAS_PROPERTY_TYPE]->(PropertyType)` edges and
  added `area` as a first-class filter in `search_auctions`; the
  four `scripts/*property_type*` / `smoke_check.py` utilities ran
  against prod to complete the migration.
- PR #10 (`claude/analyze-career-ops-project-1T3uR`) landed
  "bulletproof Neo4j Q&A": `modes/_shared.md` is now loaded into the
  system prompt, and the agent has `describe_schema`,
  and the guarded `run_cypher` escape hatch.
- PR #11 (`feature/mid-fi-ui`) replaced the Alpine.js / Tailwind UI
  with the vanilla-JS mid-fi 4-screen flow (landing / results /
  detail / watchlist) and moved the feedback control into the
  header alongside per-bubble thumbs.
- Feedback loop (`/feedback` endpoints + `resolve-feedback.yml`
  workflow) is live with context-turn capture. `sync-feedback.yml`
  now snapshots the live feed into `feedback/all.json` +
  `feedback/unresolved.json` every 15 minutes; `golden.yml` runs the
  golden-question eval nightly.
