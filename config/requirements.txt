# Full local-dev requirements (scraping + OCR + report generation).
# Production (Render) installs the leaner top-level requirements.txt instead.
# Version ranges match requirements.txt where deps overlap; ceilings sit at the
# next major to keep breaking upgrades out of a `pip install`.

# Scraping
selenium>=4.20,<5
webdriver-manager>=4.0,<5
undetected-chromedriver>=3.5,<4

# Core utilities
python-dotenv>=1.0,<2
tqdm>=4.66,<5
google-genai>=2.0,<3

# Structured extraction (pipeline/langextract_examples.py + evals/ + batch runner):
# LangExtract few-shot extraction with source grounding; `openai` is its
# OpenAI-compatible client, used to reach OpenRouter
# (LANGEXTRACT_PROVIDER=openrouter, the default). Local/offline pipeline only —
# not imported by the API server, so kept out of the production requirements.txt.
langextract>=1.5,<2
openai>=2.0,<3

pdf2image>=1.17,<2
rapidfuzz>=3.9,<4
Pillow>=10.4,<13
aiohttp>=3.10,<4
neo4j>=5.26,<7
boto3>=1.34,<2

# API + Agent layer (Phase 1)
fastapi>=0.115,<0.137  # capped: 0.137+ breaks app.routes introspection (see requirements.txt)
uvicorn[standard]>=0.30,<1.0
pydantic-ai>=2.8,<3
pyyaml>=6.0,<7

# Observability + evaluation. logfire = OpenTelemetry tracing (prod + dev);
# pydantic-evals = the golden-question eval harness (evals/, dev + CI only).
logfire[fastapi]>=4,<5
pydantic-evals>=2.8,<3

# Report generation (Phase 4)
weasyprint>=60,<70
jinja2>=3.1,<4

# Channel research (marketing/research/): Instagram + X content pulls for
# marketing content research. Local-only tooling — never installed on Render
# (see docs/marketing/channel-research.md).
instaloader>=4.14,<5
tweepy>=4.16,<5
